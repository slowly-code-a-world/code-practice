#include <iostream>
#include <vector>
#include <string>
using namespace std;
int main(void) {
	int val = -1, val1 = 1;
	cout << (val1 << 1) << " " << (val1 >> 1) << endl;
	cout << (val << 1) << " " << (val >> 1) << endl;
}
